apiVersion: 1

datasources:
- name: telegraf
  type: influxdb
  access: proxy
  url: http://${INFLUXDB_SERVICE_HOST}:8086
  isDefault: true
  editable: true
  database: telegraf
  # jsonData:
  #   timeInterval: '10s'